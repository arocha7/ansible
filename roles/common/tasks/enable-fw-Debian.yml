---

- name: Install APT UncomplicatedFW (UFW)
  apt:
    name: ufw
    state: present

- name: restart service UFW, in all cases, also issue daemon-reload to pick up config changes
  systemd:
    state: restarted
    daemon_reload: yes
    name: ufw


